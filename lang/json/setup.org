* Load json-snatcher
  #+begin_src emacs-lisp
    (load "json-snatcher")
  #+end_src


* Syntax checking
  #+begin_src emacs-lisp
    (when (executable-find "jsonlint")
      (flycheck-mode))
  #+end_src
