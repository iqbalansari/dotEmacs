* Install clojure mode, cider and ac-cider
  #+begin_src emacs-lisp
    (el-get 'sync '(clojure-mode 
                    cider 
                    ac-cider
                    clj-refactor))
  #+end_src
